Django==5.2.4
asgiref==3.10.0
sqlparse==0.5.3
 tzdata==2025.2

# Forms & UI
django-crispy-forms==2.3
crispy-tailwind==0.5.0

# Utils
python-decouple==3.8
Pillow==10.4.0
whitenoise==6.7.0

# Dev utilities (optionnel en prod)
django-extensions==3.2.3

# DÃ©ploiement / Base de donnÃ©es (Render)
# Utiliser psycopg (psycopg3) avec la roue binaire : compatible avec Python 3.13 et plus facile à installer
# Si vous utilisez PostgreSQL sur Render, gardez cette dépendance :
psycopg[binary]
# Serveur WSGI (souvent requis sur Render pour Django)
gunicorn==22.0.0